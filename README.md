# Kyros API
A high-performance API for user management and authentication.
## Installation
1. Initialize the project by running `go mod init kyros-api`.
2. Install the dependencies by running `go get`.
3. Run the project by running `go run main.go`.